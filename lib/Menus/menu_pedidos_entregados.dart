import 'package:flutter/material.dart';

class MenuPedidosEntregados extends StatelessWidget {
  const MenuPedidosEntregados({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("Menú Pedidos Entregados")));
  }
}
