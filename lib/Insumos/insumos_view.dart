import 'package:flutter/material.dart';

class InsumosView extends StatelessWidget {
  const InsumosView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("Insumos")));
  }
}
